import React from "react";

type Props = {};

const ExperienceCard = ({}: Props) => {
  return (
    <div className="bg-primary-light p-2 rounded-lg scale-100 transition-all hover:scale-105 hover:bg-opacity-70">
      ExperienceCard
    </div>
  );
};

export default ExperienceCard;
